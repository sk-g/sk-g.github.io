// Remove duplicate event listeners for abstract buttons
// The functionality is now handled in arxiv.js
document.addEventListener('DOMContentLoaded', () => {
    console.log('DOM loaded in script.js');
});
